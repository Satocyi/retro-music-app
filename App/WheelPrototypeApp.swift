// WheelPrototypeApp.swift
// 動作確認用エントリポイント。ホイール動作確認用プロトタイプ。正式アプリ名ではない。

import SwiftUI

@main
struct WheelPrototypeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
